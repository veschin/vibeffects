project:
  name: "vibeffects"

testing:
  patterns:
    files: ["**/*_test.go"]

review:
  min_score: 7
  auto_redo: true

hooks:
  pre_commit: true
  scopes: [PRD, SEED, BDD, TEST, IMPL, TASK, STATUS]
  types: [feat, add, fix, refactor, remove, update]
